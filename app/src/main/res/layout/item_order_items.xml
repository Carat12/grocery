<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingHorizontal="20dp">

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="10dp"
        android:layout_marginTop="25dp"
        android:id="@+id/card_view_product"
        app:cardElevation="0dp">
        <ImageView
            android:id="@+id/img_view_product"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:scaleType="centerCrop" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/text_view_order_item_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_text_view_order_item_count"
        android:gravity="center"
        android:layout_marginLeft="-20dp"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@+id/card_view_product"
        android:paddingHorizontal="13dp"
        android:paddingVertical="6dp"
        android:text="1"
        android:textColor="@color/bg_white"
        android:textSize="18dp" />

    <TextView
    android:id="@+id/text_view_product_info"
    android:layout_width="120dp"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="15dp"
    android:layout_below="@id/card_view_product"
    android:text="Name"
    android:textSize="15dp" />
</RelativeLayout>